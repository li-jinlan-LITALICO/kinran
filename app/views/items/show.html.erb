<% provide(:title, @item.name) %>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @item.name %>
        <%= gravatar_for @item %>
        <%= @item.per_cost %>
        <%= @item.description %>
        <%= @item.stock_number %>
      </h1>
    </section>
  </aside>
</div>
